# Classic Frame Assets

Place the **classic ornate frame** and matching chalkboard background textures in this folder.

Suggested files:
- `frame.png` – transparent PNG of the outer frame.
- `board.jpg` – background texture for the chalkboard.
- `style.css` – frame-specific layout tweaks (loaded by the plugin in a future version).
